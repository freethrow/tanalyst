# Django Framework
Django
django-mongodb-backend>=5.1.2
django-htmx
django-celery-beat
django-redis
whitenoise

# Database & Caching
pymongo>=4.0.0
redis

# Task Queue
celery
vine<6.0.0

# Web Scraping
scrapy
pytz

# PyTorch CPU-only (MUCH faster download - ~200MB instead of ~1.5GB)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchaudio==2.2.0+cpu
torchvision==0.17.0+cpu

# AI & ML (after PyTorch to use CPU version)
pydantic>=2.0.0
pydantic-ai
sentence-transformers>=2.3.0
numpy>=1.24.0,<2.0.0

# PDF Generation
reportlab

# Email
resend

# Utilities
python-dotenv
pandas

# Development Tools
ruff

# Production Servers
gunicorn